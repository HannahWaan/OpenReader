import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App skeleton loads', (WidgetTester tester) async {
    // Placeholder test - will expand in Phase 2
    expect(1 + 1, equals(2));
  });
}
